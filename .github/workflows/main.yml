name: ci

on:
  push:
    branches: master
    
env:
  HELM_EXPERIMENTAL_OCI: 1 #enable OCI support
  GCR_IMAGE: ghcr.io/${{ github.repository }}
  
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    
      - name: Checkout Repository
        uses: actions/checkout@v2
        
      - name: install helm
        uses: Azure/setup-helm@v1
        
      - name: login to acr using helm
        run: |
          echo ${{ secrets.GITHUB_TOKEN }} | helm registry login ${{ env.GCR_IMAGE }} --username ${{ github.repository_owner }} --password-stdin
#       - name: Login to GitHub Container Registry
#         uses: docker/login-action@v1
#         with:
#           registry: ghcr.io
#           username: ${{ github.repository_owner }}
#           password: ${{ secrets.GITHUB_TOKEN }}
          
#       - name: save helm chart to local registry
#         run: |
#           helm chart save ${{ github.workspace }}/helm-app/ ${{ env.GCR_IMAGE }}:${{ github.sha }}

#       - name: publish chart to acr
#         run: |
#           helm chart push ${{ env.GCR_IMAGE }}:${{ github.sha }}
        
        

      
      - name: Save Helm Chart 
        run: |
          helm chart save helm-app ghcr.io/${{ github.repository }}:latest
        
      - name: Push Helm Chart to GHCR
        run: |
          helm chart push ghcr.io/${{ github.repository }}:latest
          
        
